@page "/admin/ticketdetail/{ticketid:int}"
@using BethanysPieShop.Model
@using Microsoft.AspNetCore.Authorization

@attribute [Authorize(Roles = "Administrator")]

@if (Ticket != null)
{
    <h3>Support Ticket @Ticket.Id</h3>

    @if (!IsSaved)
    {
        <h3>@Ticket.Title</h3>

        <div class="row mb-3">
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Ticket ID</label>
                <div class="col-sm-8">
                    <label type="text" class="form-control-plaintext">@Ticket.Id</label>
                </div>
            </div>

            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Summary</label>
                <div class="col-sm-8">
                    <label type="text" class="form-control-plaintext">@Ticket.Summary</label>
                </div>
            </div>

            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Customer score (0-5)</label>
                <div class="col-sm-8">
                    <label type="text" readonly class="form-control-plaintext">@Ticket.CustomerSentimentScore</label>
                </div>
            </div>

            @foreach (var message in Ticket.TicketMessages)
            {
                <div class="form-group row">
                    <label class="col-sm-3 col-form-label">Original message</label>
                    <div class="col-sm-8">
                        <label type="text" readonly class="form-control-plaintext">@message.Message</label>
                    </div>
                </div>
            }

        </div>


        <h4>Add extra message</h4>

        <EditForm Model="TicketMessage" OnSubmit="OnSubmit" Enhance FormName="AddMessageToTicketForm">

            <div class="row mb-3">
                <label for="Message" class="col-form-label col-md-3">Extra message: </label>
                <div class="col-md-8">
                    <InputTextArea id="Message" class="form-control col-md-8" @bind-Value="@TicketMessage.Message" placeholder="Enter your message"></InputTextArea>
                </div>
            </div>

            <div>
                <button type="submit" class="btn btn-outline-primary ms-1">Submit</button>
            </div>

        </EditForm>
    }
    else
    {
        <h4 class="text-success">@Message</h4>
    }
}